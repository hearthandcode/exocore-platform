export const APP_VERSION = "0.0.1";
export const RELEASE_LABEL = "pre-alpha";

export const PRODUCT_LIMITS = [
  "Mock adapter only",
  "No network or provider calls",
  "No credentials",
  "Scores are fixture-bounded evidence",
] as const;

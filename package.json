{
  "name": "exocore-platform",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "A local-first pre-alpha desktop workbench for governed agent and profile evaluation.",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 1420",
    "build": "tsc && vite build",
    "check": "tsc --noEmit",
    "check:boundaries": "node contracts/foundation/check-boundaries.mjs",
    "format:check": "prettier --check \"src/foundation/**/*.ts\" src/form-intake-registry/index.ts src/form-intake-registry/module.ts src/form-intake-registry/types.ts src/form-intake-registry/internal/hash.ts src/form-intake-registry/internal/store.ts \"src/form-intake-registry/adapters/**/*.ts\" \"src/form-intake-registry/ports/**/*.ts\" \"src/integration/**/*.ts\" src/main.ts \"tests/integration/**/*.ts\" package.json eslint.config.js .github/workflows/ci.yml \"contracts/foundation/**/*.{json,mjs}\" contracts/form-intake-registry/v1/module-manifest.json contracts/form-intake-registry/v1/config.schema.json contracts/form-intake-registry/v1/default.config.json contracts/form-intake-registry/v1/proof.workflow.json \"docs/foundation/**/*.{md,yaml}\" docs/form-intake-registry/mount-readiness.md \"docs/architecture/**/*.md\" \"docs/integration/**/*.md\" README.md docs/ARCHITECTURE.md",
    "lint": "eslint src tests vite.config.ts",
    "test": "vitest run",
    "test:form-intake-registry": "vitest run tests/form-intake-registry",
    "test:integration": "vitest run tests/integration",
    "validate:intake-registry": "node scripts/validate-intake-registry-projection.mjs",
    "validate:artifact-surface": "node scripts/validate-artifact-surface-projection.mjs",
    "validate:integration-contracts": "python3 scripts/validate_integration_contracts.py",
    "proof:quick": "node scripts/proof.mjs --quick",
    "proof": "node scripts/proof.mjs",
    "preview": "vite preview --host 127.0.0.1 --port 4173",
    "tauri": "tauri"
  },
  "dependencies": {
    "@noble/hashes": "2.0.1",
    "@tauri-apps/api": "^2",
    "xstate": "^5.32.5",
    "zustand": "^5.0.14"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@types/node": "^26.2.0",
    "eslint": "^9.39.5",
    "prettier": "^3.9.6",
    "typescript": "~5.8.3",
    "typescript-eslint": "^8.66.0",
    "vite": "^7.0.4",
    "vitest": "^4.1.10"
  }
}

{
  "name": "exocore-platform",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "A local-first pre-alpha desktop workbench for governed agent and profile evaluation.",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 1420",
    "build": "tsc && vite build",
    "check": "tsc --noEmit",
    "check:boundaries": "node contracts/foundation/check-boundaries.mjs",
    "format:check": "prettier --check \"src/foundation/**/*.ts\" src/main.ts package.json eslint.config.js .github/workflows/ci.yml \"contracts/foundation/**/*.{json,mjs}\" \"docs/foundation/**/*.{md,yaml}\" README.md docs/ARCHITECTURE.md",
    "lint": "eslint src/foundation src/main.ts vite.config.ts",
    "test": "vitest run",
    "preview": "vite preview --host 127.0.0.1 --port 4173",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "xstate": "^5.32.5",
    "zustand": "^5.0.14"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "eslint": "^9.39.5",
    "prettier": "^3.9.6",
    "typescript": "~5.8.3",
    "typescript-eslint": "^8.66.0",
    "vite": "^7.0.4",
    "vitest": "^3.2.7"
  }
}
